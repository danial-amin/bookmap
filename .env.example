# Copy to .env.local for local dev (Vite loads VITE_* vars)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Railway (optional — only if you run server/)
PORT=3000
